[tool.poetry]
name = "intellicrack"
version = "0.1.0"
description = "Advanced binary analysis and licensing cracking platform"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

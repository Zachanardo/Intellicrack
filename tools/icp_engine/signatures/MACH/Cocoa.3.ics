// Intellicrack Protection Engine: detection rule file

init("library", "Cocoa");

function detect() {
    if (MACH.isLibraryPresent("Cocoa")) {
        bDetected = true;
    }

    return result();
}
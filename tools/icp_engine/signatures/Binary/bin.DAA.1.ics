// Intellicrack Protection Engine: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// Part of ICP Engine detection signatures
init("format", "Direct Access Archive (.DAA)");

function detect() {
    if (Binary.compare("'DAA'")) {
        bDetected = true;
    }

    return result();
}

repos:
  # Note: Test hooks disabled until test infrastructure is fixed
  # - repo: local
  #   hooks:
  #     - id: test-coverage
  #       name: Check test coverage (95% minimum)
  #       entry: C:/Intellicrack/mamba_env/python.exe -m pytest --cov=intellicrack --cov-fail-under=95 --cov-report=term-missing
  #       language: system
  #       pass_filenames: false
  #       always_run: true
  #       stages: [pre-commit]

  #     - id: no-mock-validation
  #       name: Verify no mock/fake data in tests
  #       entry: C:/Intellicrack/mamba_env/python.exe scripts/verify_no_mocks.py
  #       language: system
  #       pass_filenames: false
  #       always_run: true
  #       stages: [pre-commit]

  #     - id: test-real-functionality
  #       name: Run real functionality tests
  #       entry: C:/Intellicrack/mamba_env/python.exe -m pytest tests/unit -k "not slow" --tb=short
  #       language: system
  #       pass_filenames: false
  #       stages: [pre-commit]

  # Code Quality - Advanced Linting & Formatting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # Security - Secret Scanning
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']

  # Security - Vulnerability Scanning
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.10
    hooks:
      - id: bandit
        args: ['-r', 'intellicrack/', '-f', 'json', '-o', 'bandit-report.json']
        exclude: ^tests/

  # Basic Git Hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-merge-conflict
      - id: check-added-large-files
        args: ['--maxkb=10000']
      - id: check-ast
      - id: debug-statements

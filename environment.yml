# Minimal environment configuration for CI/CD
# All Python dependencies are managed in pyproject.toml
# This file only sets up the base Python environment
name: intellicrack
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
